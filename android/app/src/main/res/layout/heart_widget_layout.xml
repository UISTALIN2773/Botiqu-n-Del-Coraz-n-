<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="10dp"
    android:background="@drawable/widget_bg_card">

    <!-- Esquina Superior Derecha: Insignia circular en relieve con icono de corazón carmesí con glow -->
    <FrameLayout
        android:layout_width="26dp"
        android:layout_height="26dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:background="@drawable/widget_badge_glow">

        <ImageView
            android:id="@+id/widget_heart_icon"
            android:layout_width="14dp"
            android:layout_height="14dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_heart"
            android:contentDescription="Heart Badge" />
    </FrameLayout>

    <!-- Contenedor Principal Centrado -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center_horizontal|center_vertical">

        <!-- Zona Superior Central: Contenedor de imagen circular con borde perimetral de 2dp en tono crema -->
        <FrameLayout
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@drawable/widget_photo_border"
            android:padding="2dp">

            <ImageView
                android:id="@+id/widget_photo"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:src="@mipmap/ic_launcher_round"
                android:scaleType="centerCrop"
                android:contentDescription="Foto Pareja" />
        </FrameLayout>

        <!-- Zona Tipográfica Intermedia -->
        <!-- Línea Superior: Mi Amor • Día 450 Juntos ❤️ en blanco puro #FFFFFF -->
        <TextView
            android:id="@+id/widget_author_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Mi Amor • Día 450 Juntos ❤️"
            android:textSize="11sp"
            android:textStyle="bold"
            android:textColor="#FFFFFF"
            android:layout_marginTop="4dp"
            android:gravity="center"
            android:maxLines="1"
            android:ellipsize="end" />

        <!-- Línea Inferior: Texto rotativo en tono marfil suave #E6D5C3 -->
        <TextView
            android:id="@+id/widget_quote_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Tócame si me extrañas o tienes ansiedad"
            android:textSize="9sp"
            android:textColor="#E6D5C3"
            android:layout_marginTop="2dp"
            android:gravity="center"
            android:maxLines="1"
            android:ellipsize="end"
            android:paddingStart="6dp"
            android:paddingEnd="6dp" />

        <!-- Zona Inferior: Fila con Botón Play ▶ y Botón de Pánico 🚨 S.O.S. -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:layout_marginTop="6dp">

            <!-- Botón Play ▶ -->
            <LinearLayout
                android:id="@+id/widget_play_btn"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:background="@drawable/widget_play_btn_bg"
                android:gravity="center"
                android:layout_marginEnd="8dp"
                android:clickable="true"
                android:focusable="true">

                <TextView
                    android:id="@+id/widget_play_icon"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="▶"
                    android:textColor="#FFFFFF"
                    android:textSize="13sp"
                    android:textStyle="bold" />
            </LinearLayout>

            <!-- Botón de Pánico 🚨 S.O.S. -->
            <LinearLayout
                android:id="@+id/widget_sos_btn"
                android:layout_width="wrap_content"
                android:layout_height="32dp"
                android:background="@drawable/widget_sos_btn_bg"
                android:paddingStart="10dp"
                android:paddingEnd="10dp"
                android:gravity="center"
                android:clickable="true"
                android:focusable="true">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="🚨 S.O.S."
                    android:textColor="#FFFFFF"
                    android:textSize="11sp"
                    android:textStyle="bold" />
            </LinearLayout>

        </LinearLayout>

    </LinearLayout>

</RelativeLayout>
